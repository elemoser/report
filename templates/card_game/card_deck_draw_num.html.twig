{% extends "base.html.twig" %}

{% block title %}Game{% endblock %}

{% block body %}
    <h1>Drar ett antal kort från kortleken</h1>
    <p>Antal kort kvar: {{ deckCount }}</p>
    <div>
    {% for key, value in cardStrings %}
        <span class="card" style="color: {{ cardColors[key] }}">&#{{ value }}</span>
    {% endfor %}
    </div>
    <a class="button orange-button" href="{{ path('card_home') }}">Tillbaka till spelets landningssida</a>
{% endblock %}